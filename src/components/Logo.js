import React from "react";

function Logo() {
  const logoStyle = {
    fontSize: "40px",
    fontWeight: "bold",
    color: "#FF0000", // Red color
  };

  return (
    <div style={logoStyle}>
      ⨇
    </div>
  );
}

export default Logo;
